# Адресные MQTT команды для множества устройств

## Обзор

Начиная с версии **v2.3.4**, Fandomon поддерживает **адресные команды** - возможность отправлять команды конкретным устройствам, когда у вас установлен Fandomon на нескольких планшетах/устройствах.

## Как это работает

Каждое устройство Fandomon подписывается на **ДВА топика MQTT**:

1. **Широковещательный топик** (все устройства получают): `fandomon/commands`
2. **Адресный топик устройства** (только одно устройство): `fandomon/{device_id}/commands`

Где `{device_id}` - уникальный идентификатор, который вы указали в настройках Fandomon.

## Структура топиков

### Команда всем устройствам (широковещание)
```
Топик: fandomon/commands
Сообщение: {"command": "restart_fandomat"}
```
→ **ВСЕ устройства** выполнят эту команду

### Команда конкретному устройству (адресная)
```
Топик: fandomon/sklad-a/commands
Сообщение: {"command": "restart_fandomat"}
```
→ **ТОЛЬКО** устройство с `device_id = "sklad-a"` выполнит команду

## Примеры использования

### Сценарий: У вас 3 планшета в разных местах

- **Планшет 1** (Склад А): `device_id = "sklad-a"`
- **Планшет 2** (Склад Б): `device_id = "sklad-b"`
- **Планшет 3** (Офис): `device_id = "ofis"`

#### Отправить команду ВСЕМ планшетам
```bash
mosquitto_pub -h broker.hivemq.com -p 1883 \
  -u "username" -P "password" \
  -t "fandomon/commands" \
  -m '{"command": "restart_fandomat"}'
```
→ Все 3 планшета перезапустят Fandomat

#### Отправить команду ТОЛЬКО планшету на Складе А
```bash
mosquitto_pub -h broker.hivemq.com -p 1883 \
  -u "username" -P "password" \
  -t "fandomon/sklad-a/commands" \
  -m '{"command": "restart_fandomat"}'
```
→ Только планшет на Складе А перезапустит Fandomat

#### Отправить разные команды разным планшетам
```bash
# Перезапустить Fandomat на Складе А
mosquitto_pub -h broker.hivemq.com -p 1883 \
  -u "username" -P "password" \
  -t "fandomon/sklad-a/commands" \
  -m '{"command": "restart_fandomat"}'

# Остановить мониторинг на Складе Б
mosquitto_pub -h broker.hivemq.com -p 1883 \
  -u "username" -P "password" \
  -t "fandomon/sklad-b/commands" \
  -m '{"command": "stop_monitoring"}'

# Получить статус с офисного планшета
mosquitto_pub -h broker.hivemq.com -p 1883 \
  -u "username" -P "password" \
  -t "fandomon/ofis/commands" \
  -m '{"command": "send_status"}'

# Изменить интервал проверки на Складе А на 3 минуты
mosquitto_pub -h broker.hivemq.com -p 1883 \
  -u "username" -P "password" \
  -t "fandomon/sklad-a/commands" \
  -m '{"command":"UPDATE_SETTINGS","parameters":{"check_interval":"3"}}'

# Применить изменения (перезапустить мониторинг)
mosquitto_pub -h broker.hivemq.com -p 1883 \
  -u "username" -P "password" \
  -t "fandomon/sklad-a/commands" \
  -m '{"command":"restart_fandomon"}'
```

## Доступные команды

Все команды работают как с широковещательными, так и с адресными топиками:

### 1. Перезапустить Fandomat
```json
{"command": "restart_fandomat"}
```

### 2. Запустить мониторинг
```json
{"command": "start_monitoring"}
```

### 3. Остановить мониторинг
```json
{"command": "stop_monitoring"}
```

### 4. Отправить статус
```json
{"command": "send_status"}
```

### 5. Синхронизировать события
```json
{"command": "sync_events"}
```

### 6. Перезапустить Fandomon
```json
{"command": "restart_fandomon"}
```

### 7. Изменить настройки мониторинга
```json
{
  "command": "UPDATE_SETTINGS",
  "parameters": {
    "check_interval": "5",
    "status_interval": "10",
    "device_name": "Новое имя устройства"
  }
}
```

**Доступные параметры:**
- `check_interval` - интервал проверки Fandomat (в минутах)
- `status_interval` - интервал отправки статуса (в минутах)
- `device_name` - имя устройства

**Примеры:**

Изменить только интервал проверки:
```json
{"command": "UPDATE_SETTINGS", "parameters": {"check_interval": "3"}}
```

Изменить только интервал статуса:
```json
{"command": "UPDATE_SETTINGS", "parameters": {"status_interval": "15"}}
```

Изменить только имя устройства:
```json
{"command": "UPDATE_SETTINGS", "parameters": {"device_name": "Планшет Склад А"}}
```

⚠️ **ВАЖНО:** После изменения интервалов нужно перезапустить мониторинг командой `restart_fandomon` чтобы применить новые настройки:
```bash
# Шаг 1: Изменить настройки
mosquitto_pub -t "fandomon/rvm206/commands" \
  -m '{"command":"UPDATE_SETTINGS","parameters":{"check_interval":"5"}}'

# Шаг 2: Применить (перезапустить мониторинг)
mosquitto_pub -t "fandomon/rvm206/commands" \
  -m '{"command":"restart_fandomon"}'
```

## Пример на Python

```python
import paho.mqtt.client as mqtt
import json

# Настройки MQTT
BROKER = "broker.hivemq.com"
PORT = 1883
USERNAME = "ваш_логин"
PASSWORD = "ваш_пароль"

def отправить_команду_устройству(device_id, команда):
    """Отправить команду конкретному устройству"""
    client = mqtt.Client()
    client.username_pw_set(USERNAME, PASSWORD)
    client.connect(BROKER, PORT, 60)

    топик = f"fandomon/{device_id}/commands"
    сообщение = json.dumps({"command": команда})

    client.publish(топик, сообщение, qos=1)
    print(f"✅ Отправлена '{команда}' устройству '{device_id}'")

    client.disconnect()

def отправить_всем(команда):
    """Отправить команду ВСЕМ устройствам"""
    client = mqtt.Client()
    client.username_pw_set(USERNAME, PASSWORD)
    client.connect(BROKER, PORT, 60)

    топик = "fandomon/commands"
    сообщение = json.dumps({"command": команда})

    client.publish(топик, сообщение, qos=1)
    print(f"📢 Отправлена '{команда}' ВСЕМ устройствам")

    client.disconnect()

# Примеры использования
if __name__ == "__main__":
    # Команда конкретному устройству
    отправить_команду_устройству("sklad-a", "restart_fandomat")
    отправить_команду_устройству("sklad-b", "send_status")

    # Команда всем устройствам
    отправить_всем("sync_events")

    # Изменить настройки конкретного устройства
    client = mqtt.Client()
    client.username_pw_set(USERNAME, PASSWORD)
    client.connect(BROKER, PORT, 60)

    # Изменить интервал проверки на 3 минуты
    команда = {
        "command": "UPDATE_SETTINGS",
        "parameters": {
            "check_interval": "3",
            "status_interval": "5"
        }
    }
    client.publish("fandomon/sklad-a/commands", json.dumps(команда), qos=1)

    # Применить новые настройки (перезапустить мониторинг)
    client.publish("fandomon/sklad-a/commands", json.dumps({"command": "restart_fandomon"}), qos=1)

    client.disconnect()
```

## Настройка в приложении Fandomon

### Установка Device ID

1. Откройте приложение **Fandomon**
2. Перейдите в **Settings**
3. Найдите раздел **Device Settings**
4. Установите **Device ID**: введите уникальный идентификатор (например: `sklad-a`, `tablet-01`, `ofis`)
5. Установите **Device Name**: понятное имя (например: `Планшет Склад А`)
6. Убедитесь что **MQTT** включен и настроен правильно

### Проверка в логах

После установки Device ID и включения MQTT, проверьте логи:

```
📡 MQTT Command Subscription Summary:
  • Broadcast topic: fandomon/commands (for ALL devices)
  • Device-specific topic: fandomon/sklad-a/commands (for THIS device only)
  • Device ID: sklad-a
```

## Рекомендации

### 1. Используйте понятные Device ID
❌ Плохо: `device1`, `device2`, `abc123`
✅ Хорошо: `sklad-a`, `ofis-plansheta`, `liniya-1`

### 2. Формат наименований
Рекомендуемый формат: `{место}-{назначение}` или `{место}-{номер}`
- `sklad-a-glavniy`
- `ofis-priem-01`
- `zavod-liniya-3`

### 3. Когда использовать широковещание, а когда адресные команды

**Используйте широковещание** (`fandomon/commands`) когда:
- Нужно синхронизировать события на ВСЕХ устройствах
- Выполняете обслуживание всех планшетов
- Тестируете связь

**Используйте адресные команды** (`fandomon/{device_id}/commands`) когда:
- Перезапускаете конкретное проблемное устройство
- Получаете статус с одного устройства
- Управляете устройствами индивидуально

### 4. Тестирование команд

Перед использованием в работе:

1. Протестируйте на ОДНОМ устройстве используя адресные команды
2. Убедитесь что устройство отвечает правильно
3. Проверьте логи MQTT для подтверждения
4. Затем масштабируйте на множество устройств

## Мониторинг выполнения команд

### Логи MQTT на устройстве

Когда устройство получает команду, в логах будет:

**Широковещательная команда:**
```
📥 BROADCAST command received on [fandomon/commands]: {"command":"restart_fandomat"}
🔍 Parsing BROADCAST command: {"command":"restart_fandomat"}
✅ Command parsed successfully: restart_fandomat (source: BROADCAST)
```

**Адресная команда:**
```
📥 TARGETED command received on [fandomon/sklad-a/commands]: {"command":"restart_fandomat"}
🔍 Parsing TARGETED command: {"command":"restart_fandomat"}
✅ Command parsed successfully: restart_fandomat (source: TARGETED)
```

### События в ответ

После выполнения команды, устройство отправит события обратно:

**Топик событий:** `fandomon/events` (или ваш настроенный)
```json
{
  "id": 123,
  "eventType": "FANDOMAT_RESTARTING",
  "timestamp": 1729516800000,
  "message": "Attempting automatic restart of Fandomat",
  "deviceId": "sklad-a",
  "deviceName": "Планшет Склад А"
}
```

## Решение проблем

### Устройство не отвечает на адресные команды

1. **Проверьте Device ID в настройках**
   - Убедитесь что Device ID установлен правильно
   - Device ID чувствителен к регистру: `Sklad-A` ≠ `sklad-a`

2. **Проверьте MQTT подключение**
   - Убедитесь что MQTT включен
   - Проверьте broker URL, порт, логин, пароль
   - Проверьте логи подключения

3. **Проверьте формат топика**
   - Правильно: `fandomon/sklad-a/commands`
   - Неправильно: `fandomon/sklad-a/command` (без 's')
   - Неправильно: `fandomon/commands/sklad-a`

4. **Проверьте логи подписки**
   - Устройство должно логировать: "✅ Subscribed to DEVICE-SPECIFIC commands: fandomon/{id}/commands"
   - Если нет, возможно MQTT подключение не удалось

### Все устройства отвечают на адресную команду

- Возможно у нескольких устройств **одинаковый Device ID**
- Каждое устройство ДОЛЖНО иметь **уникальный** Device ID
- Измените Device ID в настройках чтобы сделать их уникальными

## Тестовый скрипт

В проекте есть интерактивный тестовый скрипт:

```bash
python test_targeted_commands.py
```

Скрипт позволяет:
- Отправлять команды конкретным устройствам
- Отправлять широковещательные команды
- Запускать демо-сценарии
- Тестировать все доступные команды

## Информация о версии

- **Функция добавлена:** v2.3.4
- **Минимальная требуемая версия:** v2.3.4+
- **Обратная совместимость:** Да, широковещательные команды работают на всех версиях

## Краткая справка

| Функция | Широковещание | Адресная команда |
|---------|---------------|------------------|
| Формат топика | `fandomon/commands` | `fandomon/{device_id}/commands` |
| Кто получает | ВСЕ устройства | ОДНО устройство |
| Назначение | Глобальные команды | Индивидуальное управление |
| Требуется Device ID | Нет | Да |
| Пример | Статус со всех | Перезапуск конкретного |

---

**Дополнительная информация:**
- [TARGETED_COMMANDS.md](TARGETED_COMMANDS.md) - Полная документация на английском
- [КОМАНДЫ_HIVEMQ.md](КОМАНДЫ_HIVEMQ.md) - Справка по командам MQTT
- [REMOTE_COMMANDS_TESTING.md](REMOTE_COMMANDS_TESTING.md) - Руководство по тестированию
